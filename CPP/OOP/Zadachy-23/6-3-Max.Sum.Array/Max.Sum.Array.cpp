/*

You are given code that reads arrays from the console and prints the array with the maximum sum of elements. The
code uses an Array class that you have to implement – make sure you handle memory management correctly.
You should submit only the file(s) you created. The Judge system has the other files and will compile them, along
with your file(s), in the same directory.
Restrictions
There will be between 1 and 1000 (inclusive) arrays in the input. Each array will have no more than 1000 elements,
and each of its elements will be a value between -100 and +100 (inclusive).

Input
3
4 1 -2 3 4
1 505
2 13 42

Output
505
*/